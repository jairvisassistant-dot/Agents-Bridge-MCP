# openspec/config.yaml
# Auto-generated by sdd-init. Do not edit manually unless you understand the impact.

schema: spec-driven

project: agent-bridge
version: 0.1.0

context: |
  Tech stack: Python 3.12+, MCP SDK (Python), SQLite+WAL (aiosqlite), Textual (TUI),
              httpx, Starlette+Uvicorn (SSE transport), uv (package manager)
  Architecture: MCP Bridge Server — layered (server → tools → state/models → SQLite),
                agent presence heartbeat (30s interval, 90s away, 900s offline),
                role-based permissions (architect/developer/default),
                3 built-in skills with allowed_tools and restrictions
  Testing: pytest 9.0.3 + pytest-asyncio, 142 tests across 10 files (3528 lines),
           unit + integration layers, no E2E, no coverage
  Style: src layout, async-first, JSON config (bridge.json), structured logging,
         conventional commits, uv_build build system

strict_tdd: true

testing:
  runner: pytest
  runner_command: uv run pytest
  framework: pytest
  layers:
    unit: true
    integration: true
    e2e: false
  coverage:
    available: false
    command: ""
  tools:
    linter: ""
    type_checker: ""
    formatter: ""

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns (Python src layout, async-first, MCP SDK)
    tdd: true
    test_command: "uv run pytest"
  verify:
    test_command: "uv run pytest"
    build_command: "uv build"
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas
